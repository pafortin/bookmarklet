javascript:(function() {  var modalStyle = "position: fixed; z-index: 9999; left: 0; top: 0; width: 80%; height: 100%; overflow: auto; background-color: rgba(0, 0, 0, 0.4); display: flex; justify-content: center; align-items: center;";  var contentStyle = "background-color: #fefefe; padding: 20px; border: 1px solid #888; width: 60%; max-height: 80%; display: flex; flex-direction: row;";  var modal = document.createElement("div");  modal.setAttribute("style", modalStyle);  var content = document.createElement("div"); var content = document.createElement("div");
  content.setAttribute("style", contentStyle);
  var text = document.createElement("p");
  text.textContent = "Cette applet vous permet de remplacer des sauts de lignes par une ',' ou l'expression booléenne OR type \"termA\" OR \"termB\" utile quand on travaille sur des synonymes ou des noms de postes ou sociétés différentes. Bon sourcing !";
  text.style.fontSize = "10px";
  text.style.marginBottom = "10px";
content.appendChild(text); content.setAttribute("style", contentStyle);  var textarea = document.createElement("textarea");  textarea.setAttribute("rows", "20");  textarea.setAttribute("cols", "40");  content.appendChild(textarea);  var buttonContainer = document.createElement("div");  buttonContainer.setAttribute("style", "display: flex; flex-direction: column; justify-content: flex-start; align-items: flex-end; margin-left: 20px;");  var replaceCommaButton = document.createElement("button");  replaceCommaButton.textContent = "Remplacer par ','";  replaceCommaButton.style.border = "1px solid #888";  replaceCommaButton.style.padding = "5px";  replaceCommaButton.style.marginBottom = "10px";  replaceCommaButton.addEventListener("click", replaceWithComma);  buttonContainer.appendChild(replaceCommaButton);  var replaceOrButton = document.createElement("button");  replaceOrButton.textContent = "Remplacer par '\" OR \"'";  replaceOrButton.style.border = "1px solid #888";  replaceOrButton.style.padding = "5px";  replaceOrButton.style.marginBottom = "10px";  replaceOrButton.addEventListener("click", replaceWithOr);  buttonContainer.appendChild(replaceOrButton);  var closeButton = document.createElement("button");  closeButton.textContent = "Fermer";  closeButton.style.border = "1px solid #888";  closeButton.style.padding = "5px";  closeButton.addEventListener("click", closeModal);  buttonContainer.appendChild(closeButton);  var copyButton = document.createElement("button");  copyButton.textContent = "Copier";  copyButton.style.border = "1px solid #888";  copyButton.style.padding = "5px";  copyButton.addEventListener("click", copyToClipboard);  buttonContainer.appendChild(copyButton);  var footer = document.createElement("div");  footer.textContent = "Fait avec amour par Pierre-André";  footer.style.fontSize = "8px";  footer.style.marginTop = "10px";  footer.style.textAlign = "center";  footer.style.width = "100%";  content.appendChild(buttonContainer);  content.appendChild(footer);  modal.appendChild(content);  document.body.appendChild(modal);  function closeModal() {    modal.remove();  }  function replaceWithComma() {    var inputData = textarea.value;    var replacedData = inputData.replace(/\n/g, ",");    textarea.value = replacedData;  }  function replaceWithOr() {    var inputData = textarea.value;    var replacedData = inputData.replace(/\n/g, "\" OR \"");    textarea.value = '"' + replacedData + '"';  }  function copyToClipboard() {    textarea.select();    document.execCommand("copy");    closeModal();  }})();
